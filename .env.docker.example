# Development-only values for docker compose. Use long, unique secrets outside local development.
JWT_SECRET=replace_with_a_long_random_jwt_secret
SESSION_SECRET=replace_with_a_long_random_session_secret

# Optional integrations; leave blank to run without recipe search or AI chat.
EDAMAM_APP_ID=
EDAMAM_APP_KEY=
GROQ_API_KEY=
